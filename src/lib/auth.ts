/**
 * Better Auth configuration — built in PHASE 3
 * (docs/phases/phase-3-auth.md). Email+password + TOTP two-factor +
 * invitation-only signup. Uses the Drizzle adapter (MySQL) and the
 * tanstackStartCookies plugin. See ADR 0002.
 */
// import { betterAuth } from 'better-auth'
// import { drizzleAdapter } from 'better-auth/adapters/drizzle'
// import { twoFactor } from 'better-auth/plugins'
// import { reactStartCookies } from 'better-auth/react-start'
// import { db } from '~/db'

// export const auth = betterAuth({ ... })

export {}
