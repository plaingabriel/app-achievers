/**
 * Production-safe migration runner (ADR 0011): prints the pending SQL, waits
 * 5s, and requires --yes to apply against prod. Implemented in PHASE 2.
 */
console.log('TODO(phase-2): implement guarded migrate. See docs/phases/phase-2-db-layer.md')
process.exit(1)
