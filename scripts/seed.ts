/**
 * First-admin bootstrap (plan.md §8). Refuses to run if any user exists.
 * Creates roles + permissions, one admin with a one-time password printed to
 * stdout, must_change_password = true. Implemented in PHASE 3.
 */
console.log('TODO(phase-3): implement seed. See docs/phases/phase-3-auth.md')
process.exit(1)
