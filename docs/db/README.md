# Database docs

One MySQL database (`achievers`) is shared by two repos: this dashboard and the Express server.

- `ownership.md` — who owns which tables and the change process.
- `error_log.md` — the cross-repo contract for the `error_log` table.

See ADR 0008 for the decision and `plan.md` §4 for full column specs.
