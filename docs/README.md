# Docs index

- **adr/** — Architecture Decision Records (why). Start at `0000-initial-plan.md`.
- **phases/** — Ordered build units (what + how). Start at `phases/README.md`.
- **runbooks/** — Operational procedures: deploy, restore, fire-drill, rotate-credentials.
- **db/** — Schema ownership rules and the cross-repo `error_log` contract.
- **architecture.md** — current-state overview (added in Phase 8).

The full iterative planning history lives in `plan.md` at the repo root until Phase 8 absorbs it.
