node_modules
.output
dist
.vite
*.log

# env / secrets — never commit
.env
.env.*
!.env.example

# generated route tree (regenerated by the router plugin)
src/routeTree.gen.ts

# drizzle local artifacts
drizzle/meta/_journal.local.json

.DS_Store

# GitHub workflows - For now, avoid CI/CD
.github